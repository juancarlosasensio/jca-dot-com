---js
{
  pagination:{
    data:"collections.wpContent",
    size: 1,
    alias: "item",
    before: function(paginationData) { 
      const notes = this.getNotes(paginationData);
      return notes;
    }
  },
  permalink:"/{{ item.slug }}/index.html",
  layout:"layouts/page",
  eleventyComputed: {
    title:"{{ item.title | safe }}"
  }  
}
---

<aside class="alert bg-dark text-light p-m">
  <p>
    Notes are a collection of my thoughts, ideas, and reflections.
    They are not polished articles. They are raw, unfiltered musings.
    Proceed at your own risk.
  </p>
</aside>

{{ item.content | safe }}

<div>
  <a href="/thinking">← Back to Notes</a>
</div>  
